# CA-ISO 电价价差预测 · 工程报告

> 2026-08-07 ｜ 全流程：数据对齐 → 特征工程 → 方向分类建模 → 业务评估 → Flask 网页交付

---

## 1. 项目背景与业务目标

**业务**：加州电力市场（CA-ISO）价差套利。交易规则：T-1 日在日前市场（DA）买卖 1 度电，T 日在实时市场（RTPD）反向平仓，赚取 **价差 = DA − RTPD**。
- 价差为负（RTPD 更高）→ 日前买、实时卖
- 价差为正（DA 更高）→ 日前卖、实时买
- 把握不足 → 观望

**目标**：预测次日（D+1）各节点、各小时的**价差方向**（并附带 DA/RTPD 价格曲线展示），据此给出买卖建议，交付本地网页（输入节点+日期 → 输出预测与建议）。

---

## 2. 数据概况

| 数据 | 内容 | 时间范围 | 说明 |
|---|---|---|---|
| `价格数据/*.xlsx` | 3 节点 DA / RTPD / 价差（$/MWh，逐小时） | 2024-01 ~ 2026-08 | 目标变量来源（只用非 -c 文件） |
| `load_CA_ISO_TAC_2DA.csv` | 官方日前负荷预测（系统级） | 2025-10 ~ 2026-08 | D+1 可直接使用的"未来已知"特征 |
| `load_CA_ISO_TAC_ACTUAL.csv` | 实际负荷（系统级） | 2025-04 ~ 2026-08 | 仅作历史滞后特征 |
| `zone_weather_hourly.csv` | 分区天气：温度/太阳辐射/100m风速 | 2025-04 ~ 2026-08 | 视为预测值，D+1 可直接使用 |
| `节点位置.xlsx` | 节点→区域映射 | — | SNLNDRO/CONTROLX→ZP26，ELCAJNGT→SP15 |

**节点**：SNLNDRO、CONTROLX（ZP26，已建模）；ELCAJNGT（SP15，数据仅 2026-03 起、训练窗口 0 样本，**未建模**）。

**对齐产物**：`master.csv`（45780 行长表）→ `features.parquet`（特征矩阵）。

---

## 3. 预测方法

### 3.1 任务定义与防泄漏
- 每个样本 = **决策日 D 的某个小时**，目标 = 预测 **D+1 同小时** 的价差。
- **防泄漏红线**：决策时点 D 日 13:00 前，**D 日当天的实际价格/负荷不可见** → 滞后特征只用 D-1 及更早；D+1 的 2DA 负荷与天气是预报值、此时已可得，可作特征。
- 时间切分（按决策日，不随机）：train 2025-04~12（**13700** 样本）/ val 2026-01~05（7550）/ test 2026-06~08（3300）。

### 3.2 特征工程
共 34 个特征，分五类：
- **历史滞后**（D-1/D-2/D-7 同小时 DA/RTPD/价差、D-1 负荷、节点间联动 peer 价）
- **滚动统计**（近 7/14/30 日同小时价差均值与标准差、D-1 日内形态峰谷/波动）
- **未来已知**（D+1 的 2DA 负荷、温度、太阳辐射、风速、负荷峰值标记）
- **日历**（D+1 的星期、月份、美国节假日、光伏时段标记、小时）
- **节点**（node 编码）

### 3.3 模型
- **决策核心**：LightGBM **方向分类器**，目标 `y = (价差>0)`，直接输出 `P(价差>0)`（二分类 logloss，val 早停）。
- **展示辅助**：LightGBM 分位数回归（q10/q50/q90）画价差曲线与置信带；DA/RTPD q50 点预测画价格曲线。
- 训练用 val 早停，缺省特征由 LightGBM 原生处理 NaN。

### 3.4 决策规则（单边策略）
实测发现模型对方向信号**不对称**（卖方向命中 71%、买方向 58%，负价差方向错单是亏损主因）：
```
P(价差>0) > 0.5  且  近7日价差波动 std7 ≤ 120  →  "卖"（日前卖、实时买）
否则                                            →  "观望"
```

---

## 4. 各节点结果（test 2026-06~08 / val 2026-01~05）

| 节点 | 时段 | 交易数(占比) | 交易命中率 | 单边策略收益 | 实际正价差占比 | 价差幅度\|实际\| |
|---|---|---|---|---|---|---|
| **SNLNDRO** | val | 1931 (53%) | **69.9%** | **+5483** | 66.6% | 9.9 |
| | test | 958 (61%) | **68.0%** | **+1808** | 62.6% | 7.8 |
| **CONTROLX** | val | 19 (1%) | 10.5% | -409 | 26.3% | 106.6 |
| | test | 2 (0%) | 0% | -64 | 33.9% | 213.8 |
| **整体** | val | 1950 | 69.3% | **+5074** | — | — |
| | test | 960 | 67.8% | **+1744** | — | — |

**结论**：
- **SNLNDRO 是策略主力**：该节点价差系统性偏正（约 2/3 时间 DA>RTPD），模型预测正价差命中 68–70%，且幅度小（±10）风险可控 → 稳定盈利。
- **CONTROLX 几乎不参与**：实际正价差只占 26–34%，模型对其"预测正价差"信号弱（命中仅 10%），单边规则下几乎不触发交易，避免了大幅度节点的亏损。
- ELCAJNGT 未建模（数据不足）。

---

## 5. 影响因子占比（方向分类器 特征重要性，按 gain）

| 排名 | 特征 | 占比 |
|---|---|---|
| 1 | da_lag1（D-1 日前价） | **31.2%** |
| 2 | da_lag2（D-2 日前价） | **24.9%** |
| 3 | month_next（月份） | 4.4% |
| 4 | load_2da_next（D+1 负荷预测） | 4.2% |
| 5 | da_lag7（D-7 日前价） | 4.1% |
| 6 | t2m_next（D+1 温度） | 3.1% |
| 7 | rtpd_lag7 | 3.0% |
| 8 | wind100_next（D+1 风速） | 2.8% |
| 9 | spread_std30 | 2.4% |
| 10 | dow_next（星期） | 2.3% |

**类别汇总**：历史价格滞后 **72.8%**（其中 DA 价滞后约 60%）｜ 未来预报（负荷/天气）18.3% ｜ 日历 8.2% ｜ 滚动统计 6.9% ｜ 节点 0.7%。

**解读**：方向信号主要来自 **DA 价的滞后结构**（DA 价是 T-1 已确定的撮合价，对次日价差方向最具预示力）；其次 D+1 的负荷与天气预报（供给侧之外的系统状态）。

---

## 6. 基线对比

| 策略 | test 模拟套利收益 |
|---|---|
| **本工程（单边策略）** | **+1744** |
| 全交易（无观望/无风控） | -129655 |
| 反向策略（每笔取负） | +129655 |
| 不交易 | 0 |

**数值基线**（spread 预测）：naive（用 D-1 同小时价差直推）MAE=129.0 → 本模型 MAE=**109.1**（提升 15%）。

结论：单边策略显著优于全交易（规避了 -13 万的大亏）与 naive 基线。

---

## 7. 如何评测模型好坏

分四层，业务优先：
1. **方向准确率（核心业务指标）**：整体方向准确率（sign 预测 vs 实际）62.6%；**交易时段命中率** 67.8%（只看实际下单的样本，更贴业务）。
2. **数值精度**：spread/DA/RTPD 的 MAE、RMSE（模型 spread MAE 109.1 vs naive 129.0）——作参考，非主指标（价差方向才是业务目标）。
3. **决策质量**：买/卖/观望占比；每类决策平均收益；hold 是否避开了大额亏损。
4. **模拟套利**：按预测方向 DA 买卖、RTPD 反向平仓、按真实价差结算 → 总收益、日均、命中率、**最大单笔亏损**、累计收益曲线；对比全交易/反向/不交易。
5. **稳健性**：val（2026 H1）与 test（2026 H2）双时段都为正收益（+5074 / +1744），避免只在测试集上调参的过拟合。

---

## 8. 已知局限（如实说明）

1. **套利利润偏薄**：val +5074 / test +1744，日均约 27–60——价差本身难预测，方向 62.6% 带来的收益空间有限。
2. **单边牺牲双边机会**：只做"卖"（正价差）方向，负价差方向的套利机会被放弃。
3. **交易集中在 SNLNDRO**：CONTROLX 在单边下几乎不交易。
4. **跨时段仍有漂移**：val/test 收益均为正但绝对值不同，市场状态变化仍影响表现。
5. **决策阈值基于回测校准**（prob>0.5、std7≤120），实盘需再验证。
6. **ELCAJNGT 未训练**；天气按"预报值"假设（若实测需改滞后特征）。

---

## 9. 增强方向（需外部数据，按优先级）

| 数据 | 为什么有用 | 当前缺失 |
|---|---|---|
| **天然气价格**（Henry Hub / 加州气价） | 燃气机组是加州边际定价者，电价 ≈ 气价×热耗率；DA/RTPD 用不同时点气价 → 直接是价差方向的供给侧信号 | 缺（历史价滞后只间接含过去气价，无 D+1 气价预测） |
| **CA-ISO 光伏/风电出力预测** | 可再生能源出力直接影响 RTPD 与价差（鸭子曲线核心驱动） | 缺（ssrd 只是代理，非实际出力） |
| **负荷偏差**（实时 vs 日前预测） | 日内偏差是 RTPD 波动的直接诱因 | 缺 |
| **更多节点/区域价格** | 阻塞与区域价差预示方向 | 仅 2 节点 |
| **滚动/更长历史训练** | 适应市场漂移 | 已扩至 9 个月，仍可加滚动窗口 |

---

## 10. 交付物

| 文件 | 作用 |
|---|---|
| `code/read_data.py` | 数据对齐 → master.csv |
| `code/features.py` | 特征工程 + 时间切分 + 防泄漏校验 |
| `code/train.py` | 方向分类器 + 分位数回归训练，输出模型与测试预测 |
| `code/evaluate.py` | 业务评估（方向/决策/套利）→ 汇总 JSON + 收益曲线 |
| `code/app.py` + `code/templates/index.html` | Flask 网页：输入节点+日期 → 24h 曲线 + 买卖建议 + 回测对比 |
| `code/data/`, `code/models/` | 特征矩阵、模型、评估产物 |
| `README.md` | 运行方法与结果摘要 |

**运行**：`python code/app.py` → http://127.0.0.1:5000
