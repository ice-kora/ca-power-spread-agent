# CAISO 电价价差项目 · 数据审计与业务口径

> 2026-08-07 ｜ 只读审计，未做任何建模
> **历史口径声明**：本文系 V0.1 审计记录。决策 cutoff 已由 `docs/market_timeline.md`（2026-08-09，官方 BPM）**修正为 D-1 日 10:00 PT（DAM Market Close / bid cutoff）**；13:00 是 DA 结果发布（label 可见）时点。下文相关表述已同步修正并标注。

---

## 1. 业务时间线（一条线，标注"何时可见什么"）

```
─────────────────────────────────────────────────────────────────────────────▶ 时间
 D-7 ────────── D-2 ────────── D-1（决策日） ──────────── D（交付日 D+1） ──▶
  │              │              │                        │
  │【日前市场】   │        ① 提交 bid（10:00 PT 前，bid cutoff）│
  │ 开闸(D-7)    │        ② DAM 出清 + 结果发布（13:00 PT） │
  │              │            （DA 结果发布，非 bid cutoff；│
  │              │            官方 BPM "DAM closes at 1000   │
  │              │            hours"，结果 "by 1300 hours"） │
  │              │            → D+1 的 DA 价此时确定（label）│
  │              │                                       │
  │              │                                       │【实时市场】D 日 0:00 起
  │              │                                       │  出清 RTPD（15min/5min，
  │              │                                       │  数据聚合为小时）
```

**★ 决策时点 = D-1 日 10:00 PT 之前（提交 bid 前，DAM Market Close / bid cutoff）**

| 类别 | 数据 | 时点 |
|---|---|---|
| ✅ **输入（决策时可见）** | ≤D-2 的历史价格 DA/RTPD/Return、历史负荷/天气 | 已发生 |
| | D+1 的负荷预测（2DA） | 若发布时间允许 |
| | D+1 的天气预测（t2m/ssrd/wind） | 若为预报 |
| | 节点信息、时间特征、历史统计 | 静态/日历 |
| ❌ **事后标签（决策时不可见）** | D+1 的 DA 价 | D-1 13:00（DA 结果发布） |
| | D+1 的 RTPD 价 | D 日实时 |
| | D+1 的 Return（= DA − RTPD） | 两者齐备后 |
| | D+1 实际负荷 / 实际天气 | D 日后 |

**关键结论**：预测 `D+1` 的 Return 方向，可用信息截至 **决策时点（D-1 10:00 PT 前，bid cutoff）**。任何用到 D+1 的 DA/RTPD/Return/实际负荷/实际天气 的"未来函数"都会造成泄漏——时间线即审计边界。

---

## 2. 数据字段审计（全字段/时间/粒度/值域）

### 2.1 价格 Excel（`价格数据/*.xlsx`，3 节点，只用非 `-c`）

| 字段 | 含义 | 单位 | 粒度 |
|---|---|---|---|
| Date | 交付日（仅组内首行有值，需向下填充） | — | 日 |
| B列 market（无表头） | `DA` / `RTPD` / `DARTPD Return` | — | 每市场一行 |
| Hours | 小时数（正常 24，部分日 <24） | — | — |
| Avg / Total | 当日平均 / 合计 | $/MWh | 日 |
| H1~H24 | 逐小时价格 | $/MWh | 小时 |

- **时间/节点覆盖**：SNLNDRO、CONTROLX 各 **948 天（2024-01-01~2026-08-05）**，每天 DA/RTPD/Return 各 1 行；ELCAJNGT **156 天（2026-03-03 起）**。
- **`DARTPD Return` 已实证 = DA − RTPD**（22746 样本 max 偏差 1e-13）。
- **目标变量**：`Return = DA − RTPD`（逐小时）。负值大量存在（DA 约 29% 为负、RTPD 约 17% 为负）。
- 注意：同一文件内三行顺序不固定，按 market 值分列后对齐。

### 2.2 `load_CA_ISO_TAC_2DA.csv`（日前负荷预测）

| 字段 | 含义 | 单位 | 粒度 |
|---|---|---|---|
| Date | 预测的负荷日期 | — | 日 |
| Avg / H1~H24 | 系统级负荷预测 | MW | 日/小时 |

- **500 行，日期 2025-10-01 ~ 2026-07-09**（另有 2026-07-21~08-07 段格式为 ISO，**格式混合，需统一**）。
- 性质：**预测值**（文件名 "2DA"）。**发布时点语义待确认**（是"D-2 发布"还是"该日期的日前预测"——决定 D+1 该行能否作特征）。

### 2.3 `load_CA_ISO_TAC_ACTUAL.csv`（实际负荷）

| 字段 | 含义 | 单位 | 粒度 |
|---|---|---|---|
| Date / Avg / H1~H24 | 系统级实际负荷 | MW | 日/小时 |

- **490 行，2025-04-01 ~ 2026-08-04**。
- 性质：**事后实测**（D+1 日过后才可得知）→ **禁止作 D+1 输入**，仅历史可用。

### 2.4 `zone_weather_hourly.csv`（天气）

| 字段 | 含义 | 单位 | 粒度 |
|---|---|---|---|
| zone | NP15 / SP15 / ZP26 | — | — |
| valid_pt | 小时戳（推测 00:00→H1） | — | 小时 |
| t2m_c | 2m 温度 | °C | 小时 |
| ssrd_wm2 | 地表太阳辐射（光伏代理） | W/m² | 小时 |
| wind100 | 100m 风速 | m/s | 小时 |
| Date | 日期 | — | — |

- **36216 行，3 zone，2025-04-02 ~ 2026-08-19**（延伸到未来 → **疑似预报，性质待确认**）。
- 值域：温度 -4.3~36.8°C，ssrd 0~1200 W/m²，wind 0~14.5 m/s。
- ⚠️ **关键穿越风险点**：若天气是事后实测，则 D+1 天气作特征 = 泄漏；若为预报则 D 日已发布、可用。

### 2.5 `节点位置.xlsx`

| 字段 | 内容 |
|---|---|
| 节点ID / 名称 | SNLNDRO_1_N001、CONTROLX_1_N001 → **ZP26**；ELCAJNGT_7_N001 → **SP15** |
| 类型 | node |
| 纬度/经度、制图坐标、省份 | 定位用，CA |

---

## 3. 数据可用性矩阵（日前可用 / 事后标签 / 穿越风险）

| 数据 | 事件时点 | 决策时(D-1 10:00 PT)可见? | 历史可训? | D+1 可预测? | 穿越风险 | 缺失降级 |
|---|---|---|---|---|---|---|
| **DA 价** | D+1 于 D 日 13:00（DA 结果发布） | ✅（≤D-2 历史） | ✅ | ⚠️ 目标一部分 | 中（若误用 D+1 DA） | — |
| **RTPD 价** | D+1 实时 | ❌（目标） | ✅ | ❌ | **高** | — |
| **Return=DA−RTPD** | D+1 | ❌（目标） | ✅ | ❌ | **高** | — |
| **2DA 负荷预测** | 该日（发布时点待确认） | ✅（若发布≤决策时） | ✅ | ✅ | **中**（发布时点未确认） | NaN |
| **实际负荷** | D+1 后 | ❌ | ✅ | ❌ | **高** | — |
| **天气 t2m/ssrd/wind** | D+1 | ⚠️ 取决于预报/实测 | ✅ | ⚠️ | **高**（实测则泄漏） | NaN |
| **节点信息** | 静态 | ✅ | ✅ | ✅ | 低 | — |
| **时间特征** | 日历 | ✅ | ✅ | ✅ | 低 | — |

**禁止作 D+1 输入（黑名单）**：未来 RTPD、未来 Return、未来 DA、未来实际负荷、未来实际天气（若实测）、D 日当天任何事后数据、决策后发布的信息。

---

## 4. 业务口径确认（冻结项）

### 4.1 Return 定义 — **已确认**
`Return = DA − RTPD`（即数据文件的 DARTPD Return，已实证）。

### 4.2 预测目标：回归 vs 分类 — **待确认（给出建议）**
- **建议**：**方向分类为主**（`sign(Return)`：正/负）+ **幅度回归为辅**（|Return| 或 q 分位数，用于置信度/风控）。
- 理由：业务决策本质是方向（买/卖），分类直接对齐；幅度仅支撑"把握程度"与观望。考核方要"可解释决策"，分类 + 结构化证据最贴合。
- 若考核方强调回归（预测数值），可两者都出（分类作决策、回归作展示）。

### 4.3 决策含义 — **已确认规则**
| Return | 含义 | 交易动作 |
|---|---|---|
| Return > 0（DA > RTPD） | 实时更便宜 | **日前卖、实时买**（Virtual Supply） |
| Return < 0（RTPD > DA） | 实时更贵 | **日前买、实时卖**（Virtual Demand） |
| |Return| 小 / 证据不足 | **观望（No Trade）** |

### 4.4 特征白名单（仅日前可得）
- DA 价格历史（D-1/D-2/D-7 同小时、近 N 日均值/趋势）
- 负荷预测（D+1 的 2DA + 历史负荷预测序列）
- 气象预测（D+1 温度/辐射/风速 + 历史，**前提：确认是预报**）
- 节点信息（node/zone）
- 时间特征（hour/dow/month/holiday/季节）
- 历史统计（Return 滚动均值/波动/近期方向、极端日标记）

### 4.4a 特征 ↔ 业务含义映射（为什么要这些特征）

| 特征 | 业务含义 | 对应市场机理 |
|---|---|---|
| **ssrd_wm2（太阳辐射）** | 光伏发电量代理 | 光伏出力 → 午间供给过剩（鸭子曲线）→ 午间价格/价差压力 |
| **wind100（100m 风速）** | 风电发电量代理 | 风电出力 → 供给侧扰动 → 价格与价差 |
| **t2m_c（温度）** | 用电需求代理 | 温度 → 制冷/采暖需求 → 负荷 → 价格中枢 |
| **负荷 2DA（预测）** | 系统需求预测 | 需求高 → 价格中枢上移；预测被修正 → 实时价相对日前偏差 |
| **节点 / zone** | 区域供需 + 输电约束 | 不同区域供需/阻塞差异 → 节点间价差 |
| **历史 Return 统计**（均值/波动/趋势） | 历史市场行为 | 价差惯性、趋势延续、波动→观望风控依据 |
| **时间特征**（hour/dow/month/holiday） | 日历需求形态 | 高峰小时/周末/节假日/季节的需求差异 |

> 这些特征都作用于"预期差/预测修正/突发变化"的解释，而非绝对水平规则。

### 4.5 黑名单（严禁输入）
未来 RTPD、未来 Return、未来 DA、未来实际负荷、未来实际天气（若实测）、决策时点后才发布的信息。

---

## 5. 待确认项（冻结，未确认前不实现规则方向）

1. **预测目标**：Return 方向分类 / 数值回归 / 两者结合？
2. **天气性质**：`zone_weather_hourly.csv` 是**预报**还是**事后实测**？（决定 D+1 天气可否作特征）
3. **2DA 负荷发布时点**："2DA" 具体指什么（D-2 发布？该日前日发布？）——决定其 D+1 行可否作特征。
4. **决策时点**：~~"D-1 日 13:00 前提交 bid"~~ —— **已由 `docs/market_timeline.md` 核验并修正**：bid cutoff = **D-1 日 10:00 PT（DAM Market Close，官方 BPM "DAM closes at 1000 hours"）**；13:00 是 DA 结果发布（label 可见）时点。此条已关闭，不再待确认。
5. **结算口径**：RT 用 RTPD（15min 聚合）还是 RT5M？DA 用哪个 LMP 组件？
6. **预测粒度**：逐小时（24h）还是每日整体方向？
7. **术语**：采用 Virtual Demand/Supply 还是其他表述。

---

## 6. 模型路径与交付物（确认口径后执行）

**模型顺序**（先可解释、后强模型）：
1. **规则基线**：白盒规则引擎（结构化的规则证据，不用自由"思维链"）。
2. **可解释模型**：逻辑回归 / 浅层决策树 → 输出**特征重要性 + 可解释理由**（系数/分裂路径），在 val 校准。
3. **强模型对照**：CatBoost 仅作 **ML Challenger**，不作决策唯一来源。

**交付物**：
- 数据字典 + 字段审计表（本文档即雏形，可转正式 `data_dictionary.md`）
- 特征说明（白名单特征 + 构造方法）
- 预测结果（方向 + 置信 + 可解释理由）
- 白盒解释报告（规则/模型/案例）
- 可运行 Demo（网页推理链界面）

---

**请确认 §4.2（预测目标）与 §5 待确认项，尤其天气性质与 2DA 时点**——这两项直接决定特征能否用 D+1 预报，确认后我再进入建模。
